import React from 'react'
import ReactDOM from 'react-dom/client'
import OpenScape from './OpenScape'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <OpenScape />
  </React.StrictMode>,
)
